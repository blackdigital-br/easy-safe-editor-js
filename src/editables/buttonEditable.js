import Editable from "./editable.js";

export default class ButtonEditable extends Editable {
    /**
     * 
     * @param {HTMLElement} element 
     */
    constructor(element) {
        super(element);
    }
}